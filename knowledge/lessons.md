# 持久教训（只增不删；每条可操作）

- **L-001 · 结论必须来自可复现测试**（2026-07 停摆）：散文规范无法被测试→两套实现漂移→6 交易日估值缺口。下次：任何"修好了"必须指向一个绿色测试。
- **L-002 · 财报前不动**（CORE-004 实践）：二元事件前基于价格/浮亏调仓没有正期望。下次：财报前动作只有"持有"，把决策放在财报后的**确认收盘**。
- **L-003 · 止盈止损须收盘确认**（ACT-002/003）：盘中触价是噪声源。下次：任何离场在**收盘确认**（公式收盘）印证后执行；INTRADAY（:00/:30）只标记 YELLOW/RED_PENDING_CLOSE，不据盘中触价成交。離場判定・確認は**公式收盘**で行う（2026-08-17 の口径統一で一時「T-20min価で確認」に読替えたが、「この価格は取引専用であり翌日の取引戦略分析には含まれない・取引価格と保有成本にのみ影響」との用户裁定で過剰と判断され差し戻し）。約定フィル価格が引け20分前価であることは離場判定には影響しない。
- **L-004 · 盘中网页行情不可用于交易**（2026-07-24 实测）：8 持仓+5 基准的 ≤20min 报价全天降级。下次：交易决策只用可核验价（**官方收盘**，三源零偏差可达成）；无 SLA 行情源前不假装能随意盘中交易。**分析・評価・離場判定・取価検証は公式收盘必須**（本条の原則）。**【2026-08-17 最小例外＝約定フィル限定】** CLOSINGの**約定フィル価格に限り**、≥2 核准源・偏差≤1% で検証した「T-20min盤中価（引け20分前）」で執行・記帳してよい（安全は≥2源・偏差≤1%要件で担保、引け後に再記帳しない）。この例外は**約定フィル価格と保有成本のみ**に及び、**分析・評価・離場判定・取価検証には及ばない**（それらは公式收盘）。**无差別・无检证な盘中触价トレードの禁止**は不変（＝検証を伴わない単一源盤中価での交易は依然不可）。
- **L-005 · 行业情绪≠公司破坏**（MP/BE 案例）：深回撤但无已核实公司减值时，规则不触发退出；将其显式标记 🚨 并写明"何种事实会改变结论"。
- **L-006 · 代理成本要显式标注**（2026-07-27）：缺真实成交价时用入场日官方收盘为代理，必须全链路标注 PROXY，真实数据一到即替换。
- **L-007 · 「能通过的检查」不等于「检查对了东西」**（2026-07-27 体检）：路径检查会自建目录→假 outbox 静默吞消息；存活检查写了却没接线；心跳写了「见下」却无下文。下次：每个守卫都要有一条**证明它会失败**的测试，且文档里的每个机制名必须能指到代码行。
- **L-008 · 入场要错峰、仓位看波动**（2026-07-27 复盘）：5 仓 3 天建完+等额定仓，把一周的市场状态变成了全部成本基。下次：新仓每周 ≤2 只，高波动标的减半仓位（HYPOTHESIS，攒样本再转 ACTIVE）。
- **L-009 · 集中度按论题算，不按行业算**（2026-07-27 复盘）：AI-电力叙事横跨工业/能源/科技，GICS 口径检不出 62% 的真实敞口。下次：逻辑卡里给每仓标论题标签，同论题合并对照 35% 上限。
- **L-010 · 事件后确认优于事件前押注**（2026-07-27 复盘）：规则化的 ABT（财报确认后入场）是唯一显著盈利仓；带着财报风险入场的 BE/RMBS 全部被动挨打。下次：优先在催化落地并被收盘确认后入场。
- **L-011 · 基本面超预期 ≠ 价格上涨**（2026-07-29 复盘，BE）：拥挤主题里记录季也可能被"卖事实"——BE Q2 纪录季 + 指引上调，AH 曾 +12.7%，但 7/29 正股收盘回吐全部财报涨幅、全线 PT 下调。下次：事件确认后若价格在收盘反向回吐全部事件涨幅，视为拥挤度/预期过高信号，降低对该主题的追高意愿（1 样本，攒样本考虑转 ACTIVE，见 playbook HYPOTHESIS）。
- **L-012 · 主题集中度须在建仓时按 NAV 设上限并强制**（2026-07-29 复盘）：L-009 已命名"按论题算集中度"，但只是命名、无约束——7/29 AI-电力主题仍占全书 45%，FOMC 一个宏观催化即同步击穿。退出按公司破坏门控是对的（不因回撤割肉），但那意味主题回撤无衰减穿透全部同主题仓；唯一防线在**入场端**。下次：新建模拟仓前先算同论题合并敞口，超 25% sim NAV 即拒绝或减半（拟入 playbook HYPOTHESIS，攒样本再转 ACTIVE）。
- **L-013 · 用户口径一经确立，个人判断不得静默覆盖，且须立即固化为机器检查**（2026-07-31）：$10k 均衡计划落地时，ABT 被自行保留半仓、生成视图残留 real/sim 区分——两者都是「口径在对话里、不在代码里」造成的复发型漂移。下次：用户确立的任何账务/展示口径，当次就写成 preflight/selftest 断言（本次落地 C7 仓位标准 + 视图纯度守卫）；豁免必须显式（SIZE_EXCEPTION 标记），不允许默认例外。
- **L-014 · 共识/预期数据入档时做量级 sanity check**（2026-08-05，MP）：登记的营收共识 ~$935M 与最近季度实际（Q1 $132.9M）差 7 倍仍入了档，靠「单源待核+财报前复核」门在 8/6 前才拦下（GF 共识 $95.95M，坏源 10 倍级错误）。下次：任何共识/预期数字入档时先对照最近 1–2 个季度实际值做量级校验，偏离 >3 倍即拒绝入档并换源；复核门保留为第二道防线。
- **L-015 · 同一条离场判据须在「想卖」与「不想卖」两个方向都被执行过才算规则**（2026-08-06 复盘，BE）：BE 冲高 249.45 后收 228.96（−2.29%，距日高 −8.2%），形态极像该止盈，但 ACT-003 判据（收盘跌破前日低 222.59）未满足、且回落缩量（11.84M<13.32M<18.85M）→ 不动；与 ABT 7/30 判据满足即执行止盈互为镜像。下次：遇「形态像但判据不满足」时，把判据的**具体数值**写成次日收盘触发条件登记入 alert-state，禁止凭印象决定。
- **L-016 · 部分止盈的同一时刻必须为剩余仓生成下一档数值判据**（2026-08-07 复盘，BE；ABT 7/30 同类前例）：减半执行完成后，剩余半仓一度处于「无退出条件」状态，靠事后补登才补齐。下次：任何 `止盈-减仓X%` / `止损-减仓X%` 落账时，同一事务内必须为剩余仓写入下一档触发数值（下破位 → 复评；上破位 → 破位失败），否则该笔交易视为未完成。
- **L-017 · 结算取价先看「同源自洽」，再看「跨源一致」**（2026-08-10，PWR/ETN）：T+40min 时 stockanalysis 的报价头（PWR 660.86）与其自己的历史表行（664.36，Adj.Close 仍为「-」＝未定稿）不一致，roic.ai 复制了同一个中途快照；只做跨源比对会误用 664.36/668.11，当日盈亏错 1.1%。下次：取价先验同源自洽（报价头能否由前收盘 ± 标示变动精确复现？历史行是否已定稿？成交量是否为全日量），自洽后才跨源比对；头/表分歧时取「能精确复现且成交量更高」的那个。**【適用範囲】** 本条の『定稿値必須（报价头が前收盘±变动で精确复现・历史行定稿・全日成交量）』は **評価・結算・取価検証（正式收盘）で従来どおり必須**。唯一の例外は CLOSINGの**約定フィル価格**——これは引け20分前の盤中価（T-20min価，≥2核准源・偏差≤1%・盤中タイムスタンプ）で執行するため定稿値・全日出来高を要求しないが、**フィル価格専用**であって評価・結算・検証には及ばない（それらは公式收盘の定稿値必須）。
- **L-018 · 单日 ≥5% 的持仓波动，复盘必须写明当日驱动事实，找不到就写「无当日事实」**（2026-08-10，MP）：MP +6.95% 当日唯一有日期的公司消息是 Deutsche Bank 目标价 61→58（负面），同业 USAR 为负、排除板块行情。下次：禁止用前几日的旧催化为今日涨幅背书；无事实的涨幅显式登记为「无信息波动」，不计入论点兑现的证据。
- **L-019 · 代理成本仓的「价格时机」得分不得计入方法有效性证据**（2026-08-10 选股审计）：MP 时机 B+ 与 GEV 时机 D 同源——都是 2026-07-16 收盘代理成本，只是一个落在低位一个落在高位。下次：选股审计中把代理成本仓单列，只用「按现行完整流程建的仓」（ABT/MSFT/NVDA 类）评价入场方法。
- **L-020 · 「无当日事实」是待办项，不是结论——必须次日复检并升级**（2026-08-11，MP）：8/10 POST_CLOSE 依 L-018 把 MP 的 +6.95% 登记为「无信息波动」（当日唯一有日期的消息是德银下调目标价），但次晨复检发现当日实有公司公告——**Project Swarm**（聚合盟国无人机/电机厂商需求、可预订德州 10X 磁材厂产能）。L-018 只写了「找不到就写无事实」，没写「然后呢」，于是错误归因差点沉淀进复盘并压低该仓评分。下次：任何「无当日事实」登记都必须带一个次日复检动作，复检到事实就当场把归因改写、并同步修正当日的九维打分理由；连续两日仍找不到才可定性为无信息波动。
- **L-021 · 行情页必须带缓存破坏参数，且「Market open / 盘中时间戳」即陈旧证据**（2026-08-11 实测）：晨间复核 8/10 收盘时，不带缓存参数抓 stockanalysis 返回的是**当日 12:57 PM EDT 的盘中快照**（PWR 665.69、成交仅 202,264，Adj.Close 为「-」），与已定稿的收盘 660.86／成交 710,370 相差 0.73%；带参数后同一页立即返回定稿值，stockscan.io 独立核对 0.00%。下次：抓任何行情页一律附唯一缓存参数；拿到页面先看三个陈旧标记——时间戳是否为「At close」而非「Market open」、Adj.Close 是否已填、成交量是否为全日量级；任一不满足即判为未定稿，不得入账。（本条同时是 L-017 的正面证据：当晚以「同源自洽」取头部值的判断，次日被定稿数据证实。）**【最小例外＝約定フィル限定】** CLOSINGの**約定フィル価格に限り**用いる引け20分前の盤中価（T-20min価）は**盤中価であって公式終値ではない**ため、上記三陈旧标记（At close 戳／Adj.Close 定稿／全日出来高）は**要求しない**（代わりに ≥2 核准源・偏差≤1%・盤中タイムスタンプで検証）。**この例外は約定フィル価格のみ**——評価・結算・分析・離場判定・取価検証では三陈旧标记（定稿値）は従来どおり**必須**（公式收盘の定稿値検証）。
- **L-022 · 第二源也会静默陈旧——必须先验证它自己的「前收盘」等于我方记录的上一交易日收盘**（2026-08-11 实测，stockscan.io）：结算 8/11 时 stockscan.io 对 KTOS 给出当日定稿值（63.73，0.00% 偏差），却同时对 **MSFT/ETN/ABT/NVDA/LLY 返回 7 月下旬的旧行情**（如 NVDA 223.96/前收 218.99＝8/7 的数据，LLY 1,154.97），站内指数条也停在旧值。若只做「跨源偏差 ≤1%」的机械比对，会因偏差 +2.97%/−4.94% 而误判为「冲突」并把当日全部结算降级——或更糟，在偏差恰好落在 1% 内时把陈旧价当交叉验证通过。**下次：任何第二源在参与比对前，先核对它给出的 previous close 是否精确等于我方账本记录的上一交易日收盘；不等即判该源对该标的失效并换源，不进入偏差计算。** 已固化：`integrity` 给 stockscan.io 打 `stamp_check` 标志、进 `STAMP_CHECK_REQUIRED`，selftest 加锁。**【適用範囲】** 「第二源の previous close ＝我方账本の上一交易日**正式收盘** と一致するか」の突合は **評価・結算・取価検証（正式收盘）で従来どおり必須**。唯一の例外は CLOSINGの**約定フィル価格**——引け20分前の盤中価で執行するため前收盘突合ではなく ≥2核准源・偏差≤1%・盤中タイムスタンプで検証する（**フィル価格専用**；評価・検証には従来の前收盘突合が効く）。stamp_check の CODIFIED テストは正式收盘検証口径のまま有効。
- **L-023 · L-007 复发：一个从未被构造过的守卫等于不存在**（2026-08-11）：为 L-022 补 selftest 时才发现，`CheckResult.__init__` 无条件对 expected/actual 做 `Decimal()`，而 **P4（cache-buster 缺失）与 P5（stamp 缺失）传的是 `"?v=N"/"stamp"/"缺失"` 这类字符串**——也就是说这两个溯源守卫**一旦真正触发就抛 `InvalidOperation`，从上线起从未成功报出过任何一次违规**，检出力为 0。L-007 早已写明「每个守卫都要有一条证明它会失败的测试」，但 P4/P5 上线时没有补，于是同一类缺陷在同一个文件里复发。**下次：新增任何 CheckResult 分支时，selftest 必须包含一条让该分支真正被构造并返回 `passed=False` 的用例；只断言「正常路径通过」不算数。** 已修复：非数值 expected/actual 不参与差值计算，并加锁「P5 对无 stamp 的 stockscan.io 有检出力」。

- **L-024 · 「弱いものを売り、強いものを買う」を数日周期で回すのは長期保有の逆——実証で 4/4 劣後**（2026-08-15 用户裁定，实证核验）：8/05–8/14 の五回の入れ替えについて「売った銘柄 vs 買った銘柄」を 8/14 終値で実測したところ、**測定可能な四回すべてで売った側が勝った**——RMBS +3.65% vs MSFT +1.63%（−2.0pp）／BE +4.83% vs NVDA→LNG −0.61%（−5.4pp）／GEV +7.31% vs LLY→TSM −4.20%（−11.5pp）／NVDA +3.52% vs LNG +2.34%（−1.2pp）。確定損 −808.32 はこの上に乗る追加コスト。**分差と結果に相関なし**（最大劣後 −11.5pp は +12 分処、+17 分処も劣後）＝九维总分は 1–2 週間の相対リターンに対する予測力が実証されていない。根因は時間軸の不一致：**買う理由は多年期の論点、売る理由は 5 日以内の価格**。九维の技術面/指標は価格派生なので、下げた仓が自動的に最弱に落ち、機械的に「卖弱买强」になる（売られた 4 銘柄が全て数日内に反発したのはその帰結）。**下次**：替换排序は論点分のみ（ACT-009）、建仓後 15 交易日は相対分で売らない（ACT-008、失效线は除く）；閾値の引き上げは頻度を下げるだけで根因を直さないことを明記した上で運用する。已固化：`selection.thesis_score/min_holding_gate`、selftest に**五回の履歴リプレイ回帰**（新規則で 4/5 拦截、RMBS→MSFT +17 は依然放行＝未カバーを明示）。
- **L-025 · 教訓はファイルに書いただけでは行動を変えない——同型動作は実行前に停止せよ**（2026-08-15，L-010 の三度の再発）：L-010「催化日の強い終値に飛び乗る入り方は横ばい圏の入り方に負ける」は 7 月に登記済みだったにもかかわらず、**NVDA（8/07 建仓）と LLY（8/10 建仓）が同一パターンで建てられ、同様に損失**、8/14 の TSM でようやく 3 日騰落 +1.02% ≤8% のチェックとして実際に適用された。**教訓が文書に存在しながら 3 回にわたり行動を変えなかった**——これは個別の判断ミスではなく自己学習機構の欠陥。同様に、LLY→TSM は替换门が「+5 恰好达标」と**境界であることを台帳に明記した上で機械的に執行**しており、「刚好达标」こそ最も一段深く考えるべき地点である。**下次**：①ある動作が既登記の教訓と同型なら、実行前に停止し「なぜ今回は違うのか」を明示できない限り実行しない；②任意の门の判定値が閾値ちょうどなら自動実行せず人の裁定に回す（边界即停）；③規則と実証が衝突したら実証を優先し、その場でユーザー裁定を求める。已固化：SYSTEM.md §4.1「深思原則」（INVIOLABLE）＋ `replacement_gate` の境界不放行、selftest 加锁。
